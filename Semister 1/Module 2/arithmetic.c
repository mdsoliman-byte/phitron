#include <stdio.h>
int main()
{
    // int x, y, divition;
    // x = 5;
    // y = 2;
    // divition = x / y;
    // printf("%d %d %d", x, y, divition);
    int x = 0 ;
    if (x=1)
    {
        printf("Hello ");
    }else{
        printf("no ");
    }
    
    return 0;
}