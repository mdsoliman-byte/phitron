#include <stdio.h>
int main (){
    /*
    < = Less than 
    > = grater than 
    <= = less than equal 
    >= = grater thean equal 
    != = not equal 
    == = equal 
    ex : 10 > 2 here 10 is big from 2 

    
    */
   
}