#include <stdio.h>
int main (){
    /*
    && ITS A LOGICAL OPERATOR 
    T && T = T
    T && F = F
    F && T = F
    F && F = F

    || ITS A LOGICAL OPERATOR 
    T || T= T
    T || F = T
    F || T = T
    F || F = T
    */
}